# Laboratory Oscilloscope UI

This is a code bundle for Laboratory Oscilloscope UI.

## Running the code

Run `npm install` to install the dependencies.

Run `npm run dev` to start the development server.
