oscilloscopes:
  # Mock scope – used in DEBUG=True mode and for local testing (no hardware required)
  - id: "scope-01"
    ip: "127.0.0.1"
    port: 5025
    label: "Mock Scope (debug)"
    driver: "mock"

  # Rigol DS1000Z series over LAN – uncomment and set the correct IP to use real hardware
  # - id: "rigol-01"
  #   ip: "192.168.1.100"
  #   port: 5025
  #   label: "Rigol DS1054Z"
  #   driver: "drivers.RigolDS1000.RigolDS1000"
